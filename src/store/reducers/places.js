const initialState = {
  places: [],
  selectedPlace: null
};

const reducer = (state = initalState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
